/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: [
      'fuchsia-legal-roundworm-794.mypinata.cloud',
      // Keep any other domains you might have already configured
    ],
  },
};

export default nextConfig;
